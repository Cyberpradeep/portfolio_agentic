# Ignore everything by default so only the base_pipeline tree is considered.
*

# Ignore environment files everywhere.
.env
**/.env

# Keep the ignore file itself.
!/.gitignore
!/Procfile
!/requirements.txt
!/README.md

# Allow the base_pipeline project files.
!/base_pipeline/
!/base_pipeline/**
